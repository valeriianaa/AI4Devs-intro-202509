
Código generado usando ChatGPT. El prompt utilizado fue:

Generate a simple HTML page with JavaScript code that reverses a string entered in a text input field when a button is clicked. The reversed string should be displayed below the button. 
HTML elements should be separated from the JavaScript code. 
The elements are: 
H1 that says: Reverse String 
single line text field 
button that says: Reverse 
The text that appears reversed once the button is clicked. 
All styled using Bootstrap 5 classes. Include necessary Bootstrap CDN links for CSS and JavaScript. The JavaScript code should be in a separate script called script.js

Mas un prompt para corregir un error de estilo de la página:

The page works as intended, but it is not styled properly. Bootstrap doesn't work.